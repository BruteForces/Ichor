import React from "react";
import { Center, Text } from "native-base";

const HomeScreen = ({ navigation, route }) => {
  return (
    <Center>
      <Text>Home</Text>
    </Center>
  );
};

export { HomeScreen };
